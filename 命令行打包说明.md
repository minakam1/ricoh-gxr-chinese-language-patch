# 命令行打包说明

项目仓库：<https://github.com/minakam1/ricoh-gxr-chinese-language-patch>

## Windows

Windows 分发包直接包含：

```text
GXR固件修改器.bat
GXR固件修改器.ps1
```

不需要 PyInstaller。运行：

```powershell
python packaging\build_cli.py
```

## macOS

```bash
python3.11 -m venv .venv-build
.venv-build/bin/python -m pip install -r requirements-build.txt
.venv-build/bin/python packaging/build_cli.py
```

分发包包含命令行程序和 `运行_GXR固件修改器.command`。

GitHub Actions 配置：`.github/workflows/build-cli.yml`。
