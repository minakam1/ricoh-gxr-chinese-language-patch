# 使用说明

项目仓库：<https://github.com/minakam1/ricoh-gxr-chinese-language-patch>

## Windows

把官方固件 ZIP 或文件夹拖到 `GXR固件修改器.bat` 上，检查通过后输入：

- `1`：英文替换版；
- `2`：完全解锁版。

程序会自动输出 ZIP。请先完整解压，`.bat` 和同目录的 `gxr_modifier.ps1` 不要分开。
如果文件不对，窗口不会关闭，重新拖入下一个文件即可。

## macOS

把官方固件 ZIP 或文件夹拖到 `运行_GXR固件修改器.command` 上，然后输入 `1`
或 `2`。程序会自动输出 ZIP。
如果文件不对，程序会继续等待下一个文件。

## 刷写

1. 解压生成的 ZIP。
2. 把 `SD_ROOT` 里面的 29 个文件复制到 SD 卡根目录。
3. 使用满电电池，关机后按住 `+` 和回放按钮 2–3 秒。
4. 选择“是”，按 MENU/OK，等待更新完成。
5. 英文替换版选择 `English`。
6. 完全解锁版确认一次当前语言；出现 11 种语言后，建议恢复官方 GXR 1.51。

刷写前请备份照片并保留官方恢复包。
